<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.udemy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Udemy">
        <activity android:name=".navigationComponent.NavigationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".viewModel.viewModelChallange.ViewModelChallangeActivity"></activity>
        <activity android:name=".viewModel.viewModelSample.ActivityViewModel"></activity>
        <activity android:name=".mainActivity" />
    </application>

</manifest>